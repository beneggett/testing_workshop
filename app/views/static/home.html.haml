- if current_user
  = render 'static/dashboard'
- else
  .center-align
    %h1 Improve your testing workflow

    %h2 Ready to test?
    %p
      = link_to "Sign In", sign_in_path, class: 'btn'
      %span – or –
      = link_to "Sign Up", sign_up_path, class: 'btn'

